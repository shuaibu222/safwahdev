export const metadata = {
  title: 'Challenges',
  description: 'Build real world problems to get ready for jobs',
};

export default function ChallengesLayout({ children }) {
  return <main id='challenges'>{children}</main>;
}
